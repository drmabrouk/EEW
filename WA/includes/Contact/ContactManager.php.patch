--- WA/includes/Contact/ContactManager.php
+++ WA/includes/Contact/ContactManager.php
@@ -43,36 +43,45 @@
         $action = $_GET['action'] ?? '';
         $id = intval($_GET['id'] ?? 0);

-        if ($action === 'delete' && $id) {
-            $wpdb->delete($table_name, ['id' => $id]);
-            echo '<div class="notice notice-success"><p>Message deleted.</p></div>';
-        } elseif ($action === 'view' && $id) {
-            $wpdb->update($table_name, ['status' => 'read'], ['id' => $id]);
-            $msg = $wpdb->get_row($wpdb->prepare("SELECT * FROM $table_name WHERE id = %d", $id));
-            if ($msg) {
-                echo '<div class="wrap"><h1>View Message</h1>';
-                echo '<p><strong>From:</strong> ' . esc_html($msg->full_name) . ' (' . esc_html($msg->email) . ')</p>';
-                echo '<p><strong>Subject:</strong> ' . esc_html($msg->subject) . '</p>';
-                echo '<p><strong>Date:</strong> ' . esc_html($msg->created_at) . '</p>';
-                echo '<hr><p>' . nl2br(esc_html($msg->message)) . '</p>';
-                echo '<a href="?page=wshc_contact_messages" class="button">Back to Messages</a></div>';
-                return;
+        if ($id) {
+            if ($action === 'delete') {
+                check_admin_referer('delete_message_' . $id);
+                $wpdb->delete($table_name, ['id' => $id]);
+                echo '<div class="notice notice-success"><p>Message deleted.</p></div>';
+            } elseif ($action === 'view') {
+                check_admin_referer('view_message_' . $id);
+                $wpdb->update($table_name, ['status' => 'read'], ['id' => $id]);
+                $msg = $wpdb->get_row($wpdb->prepare("SELECT * FROM $table_name WHERE id = %d", $id));
+                if ($msg) {
+                    echo '<div class="wrap"><h1>View Message</h1>';
+                    echo '<p><strong>From:</strong> ' . esc_html($msg->full_name) . ' (' . esc_html($msg->email) . ')</p>';
+                    echo '<p><strong>Subject:</strong> ' . esc_html($msg->subject) . '</p>';
+                    echo '<p><strong>Date:</strong> ' . esc_html($msg->created_at) . '</p>';
+                    echo '<hr><p>' . nl2br(esc_html($msg->message)) . '</p>';
+                    echo '<a href="?page=wshc_contact_messages" class="button">Back to Messages</a></div>';
+                    return;
+                }
             }
         }

-        $messages = $wpdb->get_results("SELECT * FROM $table_name ORDER BY created_at DESC");
-
-        echo '<div class="wrap"><h1>Contact Messages</h1>';
-        echo '<table class="wp-list-table widefat fixed striped">';
-        echo '<thead><tr><th>Name</th><th>Email</th><th>Subject</th><th>Status</th><th>Date</th><th>Actions</th></tr></thead><tbody>';
-        foreach ($messages as $msg) {
-            echo '<tr>';
-            echo '<td>' . esc_html($msg->full_name) . '</td>';
-            echo '<td>' . esc_html($msg->email) . '</td>';
-            echo '<td>' . esc_html($msg->subject) . '</td>';
-            echo '<td>' . esc_html($msg->status) . '</td>';
-            echo '<td>' . esc_html($msg->created_at) . '</td>';
-            echo '<td>';
-            echo '<a href="?page=wshc_contact_messages&action=view&id=' . $msg->id . '">View</a> | ';
-            echo '<a href="?page=wshc_contact_messages&action=delete&id=' . $msg->id . '" style="color:red;">Delete</a>';
-            echo '</td>';
-            echo '</tr>';
-        }
-        if (empty($messages)) {
-            echo '<tr><td colspan="6">No messages found.</td></tr>';
-        }
-        echo '</tbody></table></div>';
+        require_once WSHC_PLUGIN_DIR . 'includes/Contact/MessagesListTable.php';
+        $list_table = new MessagesListTable();
+        $list_table->prepare_items();
+
+        echo '<div class="wrap"><h1>Contact Messages</h1>';
+        echo '<form method="get">';
+        echo '<input type="hidden" name="page" value="' . esc_attr($_REQUEST['page']) . '" />';
+        echo '<div class="tablenav top">';
+        echo '<div class="alignleft actions">';
+        echo '<select name="filter_status">';
+        echo '<option value="">All Statuses</option>';
+        echo '<option value="unread" ' . selected($_REQUEST['filter_status'] ?? '', 'unread', false) . '>Unread</option>';
+        echo '<option value="read" ' . selected($_REQUEST['filter_status'] ?? '', 'read', false) . '>Read</option>';
+        echo '</select>';
+        echo '<input type="submit" class="button" value="Filter">';
+        echo '</div>';
+        echo '</div>';
+        $list_table->search_box('Search Messages', 's');
+        $list_table->display();
+        echo '</form></div>';
     }
